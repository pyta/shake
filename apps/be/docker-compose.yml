services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shake
    ports:
      - '5432:5432'
    volumes:
      - shake_pg_data:/var/lib/postgresql/data

volumes:
  shake_pg_data:
